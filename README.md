# Uku

Native Ukuvota prototype using Raylib and Kryon.

Build and run with Kryon:

```sh
sh vendor/kryon/scripts/kryon-app.sh build native
sh vendor/kryon/scripts/kryon-app.sh run native
sh vendor/kryon/scripts/kryon-app.sh build web
```
